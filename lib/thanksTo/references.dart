import 'package:flutter/material.dart';

class References extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Bilder aus dem Internet"),
      ),
      body: Column(
        children: <Widget>[
          GridView.builder(
            itemCount: ,
          )
        ],
      ),
    );
  }
}